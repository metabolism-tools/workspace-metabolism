# ICSE 2027 NIER 投稿稿（修正版）

> **状态（2026-08-18）**：本稿在另一AI的 4 页压缩版基础上修正，作为 ICSE 2027 NIER 投稿底稿。
> 官方要求已核实（[ICSE 2027 NIER 官方页面](https://conf.researchr.org/track/icse-2027/icse-2027-new-ideas-and-emerging-results--nier-)）：
> - **投稿截止：2026-10-23（周五），AoE (UTC-12h)**——距今约两个月。
> - 主文**严格 4 页**（含图表、表格、附录），参考文献额外 1 页；**任何阶段均不可购买额外页**。
> - **必须包含 "Future Plans" 章节**（本稿 VI 节）。
> - **双盲评审（double-anonymous）**：不得出现作者姓名、可识别身份的仓库链接等——本稿已将 GitHub URL 匿名化（投稿时用匿名链接，录用后恢复）。已做身份泄露扫描：仅剩的 GitHub 链接为第三方工具 zclean/agent-gc，不暴露作者身份，可保留。
> - NIER 定位："neither an ICSE research track submission with weaker or no evaluation nor an op-ed piece"。
>
> **LaTeX 骨架**：`nier-icse2027.tex`（acmart sigconf + anonymous 选项 + 匿名作者占位）+ `nier-references.bib`（15 条，全部核实存在）。
> **期刊版（TSE/TOSEM）长度政策未能在官方页面核实**（JS/Cloudflare），投稿前请以官方 Author Guidelines 为准；TSE 惯例约 14 页双栏 + 参考文献（超长有版面费），"无长度限制"说法未证实。
>
> **相对另一AI压缩稿的修正**：① 引用按首次出现顺序重排（[1]–[15]，删除正文未引用的 ACM-SE/Forbes/Kingbird 三条）；② SABER 引文改回已核实原文措辞；③ 删除 Future Plans 中的 AI 引用痕迹（`[8†L50-L51][12†L12-L14]`）；④ III 补回 L1–L5 范式定位句（NIER 最需要的"vision"元素）；⑤ 双盲匿名化。
> 完整期刊版（TSE/TOSEM 基稿）见 [agentic-metabolic-engineering-paper-v2.md](agentic-metabolic-engineering-paper-v2.md)（v3，26 条参考文献）。

---

# Agentic Metabolic Engineering: Managing Workspace Byproducts in AI-Driven Software Development

**Abstract** — The rapid adoption of vibe coding and agentic software development has created a new class of technical debt: the uncontrolled accumulation of workspace byproducts—caches, logs, draft files, and abandoned implementations. While recent work has identified workspace state drift as a fundamental bottleneck in agent performance, no existing framework provides a systematic, policy-driven approach to managing the lifecycle of these byproducts. We introduce **Agentic Metabolic Engineering**, a paradigm that treats the agent workspace as a living system requiring digestion, not just deletion. We define four metabolic phases—catabolism (audit), sequestration (clean), verification (verify), and anabolism (rollback)—and present `workspace-metabolism`, a zero-dependency Python CLI that implements this paradigm. Through a reproducible benchmark simulating 30 agentic loops, we show that a metabolized workspace maintains 2 active files versus 242 in an unmanaged workspace, while preserving full auditability and rollback capability. Our framework fills the gap between workspace governance during execution (Harness Engineering, Loop Engineering) and workspace cleanup after execution, offering the first verifiable, policy-driven lifecycle management for agent-generated byproducts.

**Index Terms**—agentic software engineering, technical debt, workspace management, vibe coding, file lifecycle


## I. Introduction

The emergence of vibe coding—software development through natural language prompts to AI agents—has fundamentally changed what a software workspace looks like. As of 2026, 92% of US-based developers (76% globally) use AI coding tools daily [1], and 41% of all code produced globally is AI-generated. While 93.5% of technology leaders rate AI-generated code as higher quality at review time, 78% report more production incidents once this code ships [2].

But code quality is only part of the problem. Each agentic loop—plan, execute, observe, reflect—leaves behind files: caches, logs, draft outputs, half-finished refactors, duplicated lock files. The workspace becomes a compost pile without a gardener.

Recent work has begun to recognize this problem. LemonHarness identified that "agents typically observe only tool outputs and log fragments, while the actual state changes occur in the file system" [3]. SABER shows that failures in long-horizon agents "cluster at a small slice of mutating steps," such as file deletion [4]. Workspace-Bench 1.0 established a benchmark for evaluating agents on workspace tasks with large-scale file dependencies [5]. AgentFold treats agent context as "a dynamic cognitive workspace to be actively sculpted" [6].

These works establish that **workspace management is not a minor hygiene issue but a fundamental bottleneck** in agent performance. Yet they stop at governance *during* execution or measurement *after* execution. None provides a systematic framework for managing the byproducts that accumulate *between* loops.

**Our contribution** is **Agentic Metabolic Engineering**—a paradigm that treats the agent workspace as a living system with four lifecycle phases, implemented in `workspace-metabolism`, a zero-dependency Python CLI.


## II. The Problem: Workspace Byproducts

Vibe coding creates a characteristic loop: AI generates code → errors occur → AI fixes them → new dependencies appear → previous approaches are abandoned but not removed → the cycle repeats. The result: `archive/`, `deprecated/`, `test_drafts/`, `tmp/`, half-finished refactors grow **faster than the code itself**.

Existing approaches are insufficient:

- **Direct deletion (`rm -rf`)** is **autophagy**—it burns potentially valuable intermediate matter.
- **Git** is right for tracked source, but most byproducts are untracked, high-churn, non-code assets.
- **Runtime hygiene tools** like `zclean` [7] and `agent-gc` [8] target processes or caches, but lack policy-driven classification, auditability, and rollback.

What is missing is a framework that treats workspace byproducts as having a **lifecycle**—not as waste to be blindly deleted, but as matter to be digested, tracked, and potentially reabsorbed.


## III. Agentic Metabolic Engineering

**Agentic Metabolic Engineering** is the systematic management of byproducts generated by agent-driven software workspaces. Where **Harness Engineering** [9] asks "Can the agent complete this task reliably?" and **Loop Engineering** [10] asks "Can the agent run itself without us?", **Agentic Metabolic Engineering** asks: "After the agent completes tasks, can the workspace survive the next one?" We position it as the fifth layer of the agentic engineering stack—after Prompt Engineering [11], Context Engineering [12], Harness Engineering [9], and Loop Engineering [10]—the last mile of every loop: after the agent plans, executes, observes, and reflects, something must tend to the files it leaves behind.

**The Four Phases.** Each maps to a command in `workspace-metabolism`:

| Phase | Command | Description |
|-------|---------|-------------|
| **Catabolism** | `wm audit` | Scan workspace, identify candidates |
| **Sequestration** | `wm clean` | Move expired matter to recycle area |
| **Verification** | `wm verify` | Hash-chain journal detects tampering |
| **Anabolism** | `wm rollback` | Re-inject material back into workspace |

**A Formal Framework.** We define a three-dimensional framework:

- **Metabolism Ladder (M0–M3)**: M0 = audit only; M1 = audit + dry-run plan; M2 = audit + clean (move to recycle, SHA-256 per file); M3 = full closed loop (audit + clean + verify + rollback). Most cleanup tools operate at M1 or below; our tool targets M3.
- **Policy Hierarchy (G1–G4)**: G1 = never touch; G2 = monitor; G3 = require approval; G4 = auto-process.
- **Metabolic Cycle**: Trigger (scheduled or event-driven) → Audit → Clean → Verify → (optionally) Rollback → Outcome state (active workspace + recycle area + audit journal).

**Policy as Code.** A single JSON policy file grades every path, enabling versionable, reviewable, auditable governance.

**The Micro-Metabolism Pattern.** At the end of each agentic loop, a micro-metabolism step executes: "The files just produced—keep, archive, recycle, or leave for tomorrow's digestion?" This can be a wrapper around `wm audit --json` that an agent calls before its next planning phase.


## IV. Implementation & Evaluation

`workspace-metabolism` is a zero-dependency Python CLI (3.11+) implementing the paradigm. All operations default to dry-run; destructive actions require explicit flags.

**Safety Model:** (1) Never delete during clean—files are moved; (2) Cryptographic verification—SHA-256 per file; (3) Hash-chain audit—append-only, tamper-evident; (4) Explicit purge—real deletion only in recycle area.

**Benchmark.** We constructed a reproducible benchmark simulating 30 agentic loops on two identical workspaces: one with `wm clean` after each loop, one without. Each loop generates 8 byproducts typical of AI coding sessions. The benchmark uses a fixed random seed and is checked into the repository as `examples/metabolism_benchmark.py`.

**Results.** After 30 loops:

| Metric | With Metabolism | Without Metabolism |
|--------|-----------------|-------------------|
| Active files | **2** | 242 |
| Recycle area | 240 files, verified | 0 |
| Hash-chain integrity | ✅ Passed | N/A |
| Rollback capability | ✅ Verified | N/A |

**2 vs 242.** The metabolized workspace kept exactly what it needed, moved everything else to a recyclable area with cryptographic verification, and maintained a tamper-evident audit trail; recycled files are recoverable via `wm rollback <run_id>` (verified at the byte level). The unmanaged workspace accumulated 240 byproducts.


## V. Related Work

**Vibe Coding & Technical Debt.** Waseem et al. analyzed "flow-debt tradeoffs" in vibe coding [13]. A 2025 survey identified dependency proliferation as "a structural form of technical debt" [14]. The position paper "Vibe Coding Needs Vibe Reasoning" calls out technical debt accumulation as a key limitation [15].

**Agent Workspace Governance.** LemonHarness establishes explicit execution boundaries [3]. AgentFold treats context as a dynamic cognitive workspace [6]. Workspace-Bench 1.0 benchmarks agents on workspace tasks [5]. SABER shows failures concentrate at mutating actions [4].

**Workspace Hygiene Tools.** zclean [7] and agent-gc [8] target orphaned processes and build artifacts but lack policy-driven classification, auditability, and rollback.

**Our Position.** To our knowledge, **no existing work combines policy-driven classification, recyclable cleanup with cryptographic rollback, hash-chain auditability, and native agent integration** into a unified framework. `workspace-metabolism` is the first to treat workspace byproducts as a **lifecycle**—not as waste to be deleted, but as matter to be digested, tracked, and potentially reabsorbed.


## VI. Future Plans

This section outlines the work we plan to turn this new idea into a full-length paper.

**1. Real-World Validation.** Our benchmark uses simulated agentic loops. We plan to collect longitudinal data from real AI development workflows, measuring metabolic debt accumulation and its correlation with agent performance metrics (task completion time, error rates).

**2. Workspace Health Score.** We will synthesize multiple metabolic debt metrics—active file counts, recycle area size, audit trail completeness—into a single health score, enabling CI/CD integration as a quality gate.

**3. Deeper Agent Integration.** We will extend the MCP (Model Context Protocol) server interface to enable fully autonomous micro-metabolism, where agents invoke `wm audit --json` and make cleanup decisions as part of their observation phase.

**4. Metabolic Debt Theory.** We will formalize the relationship between metabolic debt and agent performance, developing predictive models of when and how unmanaged byproducts degrade agent effectiveness.


## VII. Conclusion

The rapid adoption of vibe coding has created a new challenge: the uncontrolled accumulation of workspace byproducts that degrade agent performance. We have introduced **Agentic Metabolic Engineering** as a paradigm for addressing this challenge, defined four metabolic phases, and presented `workspace-metabolism`, a zero-dependency Python CLI. Through a reproducible benchmark, we have shown that a metabolized workspace maintains order (2 active files) while an unmanaged workspace accumulates 242 files. If Harness Engineering is the skeleton of an agent and Loop Engineering its heartbeat, then **Agentic Metabolic Engineering is its digestive system**—keeping the workspace alive for the next cycle.

The tool is open-source: [anonymous repository link — restore for camera-ready].


## References

[1] JetBrains, "2026 Developer Ecosystem Survey," 2026. [Online]. Available: https://blog.jetbrains.com/research/2026/04/which-ai-coding-tools-do-developers-actually-use-at-work/

[2] New Relic, "2026 State of AI Coding Report," Jun. 2026. [Online]. Available: https://newrelic.com/press-release/20260610

[3] K. Ren et al., "LemonHarness Technical Report," arXiv:2606.24311, 2026.

[4] "SABER: Small Actions, Big Errors — Safeguarding Mutating Steps in LLM Agents," arXiv:2512.07850, 2025.

[5] Z. Tang et al., "Workspace-Bench 1.0: Benchmarking AI Agents on Workspace Tasks with Large-Scale File Dependencies," arXiv:2605.03596, 2026.

[6] Tongyi Lab, Alibaba Group, "AgentFold: Long-Horizon Web Agents with Proactive Context Management," arXiv:2510.24699, 2025.

[7] TheStack-ai, "zclean: AI coding runtime hygiene — zombie cleanup and safe workspace cache cleanup," 2026. [Online]. Available: https://github.com/TheStack-ai/zclean

[8] W. Jeong, "agent-gc: AI agent worktree and dev artifact garbage collector," 2026. [Online]. Available: https://github.com/williamjeong2/agent-gc

[9] "AI Harness Engineering: A Runtime Substrate for Foundation-Model Software Agents," arXiv:2605.13357, 2026.

[10] "Engineering the Loops that Replace Step-by-Step Prompting," arXiv:2607.00038, 2026.

[11] S. Schulhoff et al., "The Prompt Report: A Systematic Survey of Prompting Techniques," arXiv:2406.06608, 2024.

[12] "A Survey of Context Engineering for Large Language Models," arXiv:2507.13334, 2025.

[13] M. Waseem et al., "Vibe Coding in Practice: Flow, Technical Debt, and Guidelines for Sustainable Use," arXiv:2512.11922, 2025.

[14] "A Survey of Vibe Coding with Large Language Models," arXiv:2510.12399, 2025.

[15] "Position: Vibe Coding Needs Vibe Reasoning: Improving Vibe Coding with Formal Verification," arXiv:2511.00202, 2025.
